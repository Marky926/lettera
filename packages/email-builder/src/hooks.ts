/**
 * Hooks-only entrypoint for `@lettera/email-builder`.
 *
 * Mirrors `@lettera/editor/hooks` for integrators who prefer the
 * meta-package namespace. Pulls React but not the full editor UI.
 */

export * from '@lettera/editor/hooks';
